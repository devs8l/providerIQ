{
  "name": "@provideriq/scoring",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@provideriq/shared": "workspace:*",
    "@provideriq/database": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
